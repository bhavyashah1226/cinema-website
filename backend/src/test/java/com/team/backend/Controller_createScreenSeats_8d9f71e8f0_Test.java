// Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-3.5-turbo

package com.team.backend;

import org.bson.types.ObjectId;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;
import javax.validation.Valid;
import java.util.List;

public class Controller_createScreenSeats_8d9f71e8f0_Test {

    @Autowired
    private ScreenRepository repository;

    @PostMapping("/screens")
    public Screen createScreenSeats(@Valid @RequestBody Screen screens) {
        screens.set_id(ObjectId.get());
        repository.save(screens);
        return screens;
    }

    @Test
    public void testCreateScreenSeats_Success() {
        Screen screen = new Screen();
        screen.setName("Screen 1");
        screen.setSeats(100);
        
        given()
            .contentType("application/json")
            .body(screen)
        .when()
            .post("/screens")
        .then()
            .statusCode(200)
            .body("name", equalTo("Screen 1"))
            .body("seats", equalTo(100));
    }

    @Test
    public void testCreateScreenSeats_InvalidInput() {
        Screen screen = new Screen();
        screen.setName("Screen 2");
        screen.setSeats(-10);
        
        given()
            .contentType("application/json")
            .body(screen)
        .when()
            .post("/screens")
        .then()
            .statusCode(400)
            .body("message", equalTo("Invalid input: seats must be a positive number"));
    }

}
