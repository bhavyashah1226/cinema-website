// Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-3.5-turbo

package com.team.backend;

import cucumber.api.java.en.Given;
import cucumber.api.java.en.Then;
import cucumber.api.java.en.When;
import org.bson.types.ObjectId;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.RequestBody;

import javax.validation.Valid;

import static org.junit.Assert.assertEquals;

public class Controller_createScreenSeats_8d9f71e8f0_Test {

    @Autowired
    private ScreenRepository repository;

    private Screen screens;
    private Screen createdScreen;

    @Given("^a screen with valid details$")
    public void a_screen_with_valid_details() {
        screens = new Screen();
        screens.setName("Screen 1");
        screens.setCapacity(100);
    }

    @When("^the createScreenSeats method is called$")
    public void the_createScreenSeats_method_is_called() {
        screens.set_id(ObjectId.get());
        createdScreen = repository.save(screens);
    }

    @Then("^the screen is created successfully$")
    public void the_screen_is_created_successfully() {
        assertEquals(screens, createdScreen);
    }

    @Given("^a screen with invalid details$")
    public void a_screen_with_invalid_details() {
        screens = new Screen();
        screens.setName("");
        screens.setCapacity(0);
    }

    @Then("^an exception is thrown$")
    public void an_exception_is_thrown() {
        try {
            repository.save(screens);
        } catch (Exception e) {
            assertEquals("Invalid screen details", e.getMessage());
        }
    }

}
