// Test generated by RoostGPT for test roostUnitTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

package com.team.backend;

import org.bson.types.ObjectId;
import org.junit.jupiter.api.*;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.springframework.beans.factory.annotation.Autowired;
import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.when;

public class Controller_createScreenSeats_8d9f71e8f0_Test {

  @Autowired
  private Controller controller;

  @Mock
  private Repository repository;

  private Screen screens;

  @BeforeEach
  void setUp() {
    screens = new Screen();
    screens.set_id(ObjectId.get());
  }

  @Test
  public void testCreateScreenSeats_Success() {
    when(repository.save(screens)).thenReturn(screens);
    Screen result = controller.createScreenSeats(screens);
    assertNotNull(result);
    assertEquals(result.get_id(), screens.get_id());
  }

  @Test
  public void testCreateScreenSeats_Failure() {
    when(repository.save(screens)).thenThrow(new RuntimeException("Test Exception"));
    assertThrows(RuntimeException.class, () -> controller.createScreenSeats(screens));
  }

}
