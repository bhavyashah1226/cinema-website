// Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

package com.team.backend;

import org.bson.types.ObjectId;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RestController;

import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.when;

@SpringBootTest
public class Controller_createScreenSeats_8d9f71e8f0_Test {

    @InjectMocks
    Controller controller;

    @Mock
    ScreenRepository repository;

    @Test
    public void createScreenSeatsTest_ValidInput() {
        Screen screen = new Screen();
        when(repository.save(any())).thenReturn(screen);
        Screen result = controller.createScreenSeats(screen);
        Assertions.assertNotNull(result);
        Assertions.assertEquals(result._id, screen._id);
    }

    @Test
    public void createScreenSeatsTest_InvalidInput() {
        Assertions.assertThrows(RuntimeException.class, () -> {
            controller.createScreenSeats(null);
        });
    }
}
