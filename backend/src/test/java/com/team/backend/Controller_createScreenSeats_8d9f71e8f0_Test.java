// Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-3.5-turbo

package com.team.backend;

import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;
import org.bson.types.ObjectId;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.RequestBody;

import javax.validation.Valid;

import static org.junit.Assert.assertEquals;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

public class Controller_createScreenSeats_8d9f71e8f0_Test {

    @Autowired
    private ScreenRepository repository;

    private Screen screens;
    private Screen savedScreen;

    @Given("a screen object")
    public void aScreenObject() {
        screens = new Screen();
        screens.setName("Screen 1");
        screens.setCapacity(100);
    }

    @When("the createScreenSeats method is called")
    public void theCreateScreenSeatsMethodIsCalled() {
        repository = mock(ScreenRepository.class);
        when(repository.save(screens)).thenReturn(screens);

        savedScreen = createScreenSeats(screens);
    }

    @Then("the screen object should be saved in the repository")
    public void theScreenObjectShouldBeSavedInTheRepository() {
        assertEquals(screens, savedScreen);
    }

    @Then("the screen object should have a generated id")
    public void theScreenObjectShouldHaveAGeneratedId() {
        ObjectId id = savedScreen.get_id();
        assertEquals(ObjectId.class, id.getClass());
    }

    private Screen createScreenSeats(@Valid @RequestBody Screen screens) {
        screens.set_id(ObjectId.get());
        repository.save(screens);
        return screens;
    }

}
