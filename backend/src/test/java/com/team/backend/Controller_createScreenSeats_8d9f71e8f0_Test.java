// Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-3.5-turbo

package com.team.backend;

import org.bson.types.ObjectId;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;
import javax.validation.Valid;
import java.util.List;

import static org.awaitility.Awaitility.await;
import static org.hamcrest.Matchers.equalTo;
import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertNotNull;

public class Controller_createScreenSeats_8d9f71e8f0_Test {

    @Autowired
    private ScreenRepository repository;

    @Test
    public void testCreateScreenSeats_Success() {
        // TODO: Mock the necessary dependencies

        Screen screens = new Screen();
        screens.setScreenName("Screen 1");
        screens.setCapacity(100);

        Screen createdScreen = createScreenSeats(screens);

        assertNotNull(createdScreen.get_id());
        assertEquals("Screen 1", createdScreen.getScreenName());
        assertEquals(100, createdScreen.getCapacity());

        // TODO: Verify that the screen is saved in the repository
    }

    @Test
    public void testCreateScreenSeats_InvalidInput() {
        // TODO: Mock the necessary dependencies

        Screen screens = new Screen();
        screens.setScreenName("");
        screens.setCapacity(0);

        Screen createdScreen = createScreenSeats(screens);

        // TODO: Verify that the screen is not saved in the repository
    }

    private Screen createScreenSeats(Screen screens) {
        screens.set_id(ObjectId.get());
        repository.save(screens);
        return screens;
    }
}
