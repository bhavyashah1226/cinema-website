// Test generated by RoostGPT for test roost-test using AI Type Azure Open AI and AI Model roost-gpt4-32k

package com.team.backend;

import org.bson.types.ObjectId;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.springframework.boot.test.context.SpringBootTest;
import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.verify;
import static org.mockito.Mockito.when;

@SpringBootTest
public class Controller_createScreenSeats_8d9f71e8f0_Test {

    @Autowired
    @InjectMocks
    private BasicController basicController;
    
    @Mock
    private ScreenRepository repository;
    
    @Test
    public void testCreateScreenSeats_successCase() {
        Screen screen = new Screen();
        when(repository.save(screen)).thenReturn(screen);
      
        Screen result = basicController.createScreenSeats(screen);
      
        assertEquals(screen.get_id(), result.get_id());
        verify(repository).save(screen);
    }

    @Test
    public void testCreateScreenSeats_NullCase() {
        try {
            Screen screen = null;
            basicController.createScreenSeats(screen);
        } catch(IllegalArgumentException e) {
            assertEquals("Screen object is null", e.getMessage());
        }
    }
}
