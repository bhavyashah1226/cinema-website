// Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-3.5-turbo

package com.team.backend;

import org.awaitility.Awaitility;
import org.bson.types.ObjectId;
import org.junit.Before;
import org.junit.Test;
import org.springframework.data.annotation.Id;

import java.util.ArrayList;

import static org.assertj.core.api.Assertions.assertThat;

public class Screen_getSeats_d4e6eecacd_Test {

    private Screen screen;

    @Before
    public void setup() {
        screen = new Screen();
    }

    @Test
    public void testGetSeats_ReturnsEmptyList_WhenNoSeatsAdded() {
        ArrayList expectedSeats = new ArrayList();

        Awaitility.await().untilAsserted(() -> {
            ArrayList actualSeats = screen.getSeats();
            assertThat(actualSeats).isEqualTo(expectedSeats);
        });
    }

    @Test
    public void testGetSeats_ReturnsCorrectSeats_WhenSeatsAdded() {
        ArrayList expectedSeats = new ArrayList();
        expectedSeats.add(new Seat("A1"));
        expectedSeats.add(new Seat("A2"));

        Awaitility.await().untilAsserted(() -> {
            ArrayList actualSeats = screen.getSeats();
            assertThat(actualSeats).isEqualTo(expectedSeats);
        });
    }

    @Id
    private static class Seat {
        private ObjectId id;
        private String name;

        public Seat(String name) {
            this.name = name;
        }

        public ObjectId getId() {
            return id;
        }

        public String getName() {
            return name;
        }
    }

    private static class Screen {
        private ArrayList<Seat> seats;

        public Screen() {
            this.seats = new ArrayList<>();
        }

        public ArrayList<Seat> getSeats() {
            return seats;
        }
    }
}
