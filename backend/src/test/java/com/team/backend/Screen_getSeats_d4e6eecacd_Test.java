// Test generated by RoostGPT for test roostUnitTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

package com.team.backend;

import java.util.ArrayList;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.Mock;
import org.mockito.junit.MockitoJUnitRunner;

@RunWith(MockitoJUnitRunner.class)
public class Screen_getSeats_d4e6eecacd_Test {
    @Mock
    private Screen screen;

    @Before
    public void setUp() {
        ArrayList<String> seats = new ArrayList<>();
        seats.add("A1");
        seats.add("B1");
        given(screen.getSeats()).willReturn(seats);
    }

    @Test
    public void testGetSeatsSize() {
        Assert.assertEquals(2, screen.getSeats().size());
    }

    @Test
    public void testGetSeatsContent() {
        ArrayList<String> actualSeats = screen.getSeats();
        Assert.assertEquals("A1", actualSeats.get(0));
        Assert.assertEquals("B1", actualSeats.get(1));
    }
}
