// Test generated by RoostGPT for test roost-test using AI Type Azure Open AI and AI Model roost-gpt4-32k

package com.team.backend;

import org.junit.*;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;
import java.util.ArrayList;
import static org.junit.Assert.*;

@RunWith(SpringRunner.class)
@SpringBootTest
public class Screen_getSeats_d4e6eecacd_Test {

    @Test
    public void testGetSeats() {
        // TODO: create and set the seats ArrayList
        ArrayList<String> seatsExpected = new ArrayList<>();
        seatsExpected.add("A1");
        seatsExpected.add("A2");

        Screen screen = new Screen(); 
        screen.setSeats(seatsExpected);

        ArrayList<String> seatsActual = screen.getSeats();

        // verify that the expected output matches the actual output
        assertEquals(seatsExpected, seatsActual);
    }

    @Test
    public void testGetSeatsEmpty() {
        // TODO: create and set an empty seats ArrayList
        ArrayList<String> seatsExpected = new ArrayList<>();

        Screen screen = new Screen();
        screen.setSeats(seatsExpected);

        ArrayList<String> seatsActual = screen.getSeats();

        // verify that the expected output matches the actual output
        assertTrue(seatsActual.isEmpty());
    }
}
