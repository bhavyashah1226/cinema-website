// Test generated by RoostGPT for test roost-test using AI Type Azure Open AI and AI Model roost-gpt4-32k

package com.team.backend;

import org.bson.types.ObjectId;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

public class Screen_get_id_c3fa75ed0b_Test {

    @Mock
    ObjectId _id;

    public String get_id() {
        return _id.toHexString();
    }

    @BeforeEach
    void setUp() {
        MockitoAnnotations.initMocks(this);
    }

    @Test
    public void testGetId_When_ID_Exists() {
        String expected_id = "507f1f77bcf86cd799439011";
        _id = new ObjectId(expected_id);
        String actual_id = get_id();
        Assertions.assertEquals(expected_id, actual_id);
    }

    @Test
    public void testGetId_When_ID_Does_Not_Exist() {
        Exception exception = Assertions.assertThrows(NullPointerException.class, this::get_id);
        String expectedMessage = "Cannot invoke \"org.bson.types.ObjectId.toHexString()"
                + " because \"this._id\" is null";
        String actualMessage = exception.getMessage();
        Assertions.assertEquals(expectedMessage, actualMessage);
    }
}
