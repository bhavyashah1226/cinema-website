// Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-3.5-turbo

package com.team.backend;

import org.bson.types.ObjectId;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;
import java.util.concurrent.TimeUnit;
import static org.awaitility.Awaitility.await;

@RunWith(SpringRunner.class)
@SpringBootTest
public class Screen_get_id_c3fa75ed0b_Test {

    private Screen screen;

    @Test
    public void testGetId_Success() {
        // TODO: Mock the ObjectId for _id.toHexString() method
        ObjectId mockId = new ObjectId("609c6e6c6d4c8e001f3a7b50");
        
        screen = new Screen(mockId);
        
        await().atMost(5, TimeUnit.SECONDS).until(() -> screen.get_id() != null);
        
        String expectedId = "609c6e6c6d4c8e001f3a7b50";
        String actualId = screen.get_id();
        
        Assert.assertEquals("The expected id should match the actual id", expectedId, actualId);
    }

    @Test
    public void testGetId_NullId() {
        // TODO: Mock the ObjectId for _id.toHexString() method
        ObjectId mockId = null;
        
        screen = new Screen(mockId);
        
        await().atMost(5, TimeUnit.SECONDS).until(() -> screen.get_id() != null);
        
        String expectedId = null;
        String actualId = screen.get_id();
        
        Assert.assertEquals("The expected id should match the actual id", expectedId, actualId);
    }
}

class Screen {
    @Id
    private ObjectId _id;

    public Screen(ObjectId _id) {
        this._id = _id;
    }

    public String get_id() {
        return _id.toHexString();
    }
}
