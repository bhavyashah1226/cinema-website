// Test generated by RoostGPT for test roost-test using AI Type Azure Open AI and AI Model roost-gpt4-32k

package com.team.backend;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;
import static org.junit.Assert.*;
import java.util.ArrayList;

@RunWith(SpringRunner.class)
@SpringBootTest
public class Screen_setSeats_f21a892dcf_Test{

    @Test
    public void testSetSeats() {
        Screen screen = new Screen();
        
        // Test with Non-empty ArrayList
        ArrayList<String> seats1 = new ArrayList<>();
        seats1.add("A1");
        seats1.add("A2");
        screen.setSeats(seats1);
        assertEquals(seats1, screen.getSeats());

        // Test with empty ArrayList
        ArrayList<String> seats2 = new ArrayList<>();
        screen.setSeats(seats2);
        assertEquals(seats2, screen.getSeats());
    }

    @Test(expected = NullPointerException.class)
    public void testSetSeatsWithNull() {
        Screen screen = new Screen();
        
        // Test with null
        screen.setSeats(null);
    }
}
