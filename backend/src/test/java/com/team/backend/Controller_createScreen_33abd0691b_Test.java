// Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

package com.team.backend;

import org.bson.types.ObjectId;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;

import static org.mockito.Mockito.*;

@SpringBootTest
public class Controller_createScreen_33abd0691b_Test {

    @Autowired
    private ScreenController screenController;

    @MockBean
    private ScreenRepository screenRepository;

    @Test
    public void testCreateScreen() {
        Screen screen = new Screen();
        screen.setName("Screen-A");
        screen.set_id(ObjectId.get());

        when(screenRepository.save(any())).thenReturn(screen);

        Screen createdScreen = screenController.createScreen(screen);

        verify(screenRepository, times(1)).save(screen);
        assert createdScreen.getName().equals(screen.getName());
    }

    @Test
    public void testCreateScreenNull() {
        Screen screen = new Screen();

        when(screenRepository.save(screen)).thenThrow(new IllegalArgumentException());

        try {
            screenController.createScreen(screen);
        } catch (IllegalArgumentException ex) {
            assert true;
        }

        verify(screenRepository, times(1)).save(screen);
    }
}
