// Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-3.5-turbo

package com.team.backend;

import org.bson.types.ObjectId;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;
import javax.validation.Valid;
import java.util.List;

public class Controller_createScreen_33abd0691b_Test {

    @Autowired
    private ScreenRepository repository;

    @Test
    public void testCreateScreen_Success() {
        // Arrange
        Screen screens = new Screen();
        screens.setName("Test Screen");
        screens.setSize(50);
        
        // Act
        Screen createdScreen = createScreen(screens);
        
        // Assert
        assertNotNull(createdScreen.get_id());
        assertEquals(screens.getName(), createdScreen.getName());
        assertEquals(screens.getSize(), createdScreen.getSize());
    }

    @Test
    public void testCreateScreen_NullInput() {
        // Arrange
        Screen screens = null;
        
        // Act
        Screen createdScreen = createScreen(screens);
        
        // Assert
        assertNull(createdScreen);
    }
    
    private Screen createScreen(@Valid @RequestBody Screen screens) {
        screens.set_id(ObjectId.get());
        repository.save(screens);
        return screens;
    }
}
