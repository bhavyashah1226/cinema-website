// Test generated by RoostGPT for test roostUnitTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

package com.team.backend;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.when;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.springframework.boot.test.context.SpringBootTest;
import org.bson.types.ObjectId;
import com.team.backend.Screen;
import com.team.backend.Repository;

@SpringBootTest
public class Controller_createScreen_33abd0691b_Test {

    @InjectMocks
    Controller controller;

    @Mock
    Repository repository;

    @Test
    public void testCreateScreenSuccess() {
        // TODO: Provide a valid screen object for test case
        Screen screen = new Screen();
        ObjectId objectId = new ObjectId();
        
        when(repository.save(screen)).thenReturn(screen);
        
        screen.set_id(objectId);

        Screen result=controller.createScreen(screen);
        
        assertNotNull(result);
        assertEquals(objectId, result.get_id());
    }

    @Test
    public void testCreateScreenFailure() {
         // TODO: Provide an invalid screen object for test case
        Screen screen = new Screen();
        ObjectId objectId = null;

        when(repository.save(screen)).thenReturn(screen);
        
        screen.set_id(objectId);

        Screen result = controller.createScreen(screen);
        
        assertNull(result.get_id());  
    }
}
