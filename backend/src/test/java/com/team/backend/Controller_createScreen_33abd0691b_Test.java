// Test generated by RoostGPT for test roost-test using AI Type Azure Open AI and AI Model roost-gpt4-32k

package com.team.backend;

import org.bson.types.ObjectId;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;

import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.when;

@ExtendWith(MockitoExtension.class)
@SpringBootTest
public class Controller_createScreen_33abd0691b_Test {

    @Mock
    Screen screens;

    @MockBean
    ScreenRepository repository;

    @Test
    public void createScreen_Successful() {

        when(repository.save(any(Screen.class))).thenReturn(screens);

        ScreenController sc = new ScreenController(repository);

        Screen screen = new Screen("A New Screen");
        Screen returnedScreen = sc.createScreen(screen);

        assertEquals(screen.get_id(), returnedScreen.get_id());
    }

    @Test
    public void createScreen_Unsuccessful_Null() {
        
        when(repository.save(null)).thenThrow(new RuntimeException("Screen cannot be null"));

        ScreenController sc = new ScreenController(repository);

        try {
            Screen returnedScreen = sc.createScreen(null);
        } catch (RuntimeException e) {
            assertEquals("Screen cannot be null", e.getMessage());
        }
    }
}
