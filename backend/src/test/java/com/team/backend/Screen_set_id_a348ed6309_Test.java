// Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

package com.team.backend;

import org.bson.types.ObjectId;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class Screen_set_id_a348ed6309_Test {

    @Test
    public void testSetId_ValidId() {
        ObjectId testId = new ObjectId();
        Screen screen = new Screen();
        screen.set_id(testId);
        Assertions.assertEquals(testId, screen.get_id(), "The ID does not match the expected value");
    }

    @Test
    public void testSetId_NullId() {
        Screen screen = new Screen();
        
        // TODO: Replace with the actual exception that the method throws in case the Id is null
        Assertions.assertThrows(IllegalArgumentException.class, () -> {
            screen.set_id(null);
        });
    }

    @Test
    public void testSetId_InvalidId() {
        Screen screen = new Screen();
        
        // TODO: Replace "invalid_id" with an actual invalid ObjectId
        ObjectId invalidId = new ObjectId("invalid_id");
        
        // TODO: Replace with the actual exception that the method throws in case the Id is invalid
        Assertions.assertThrows(IllegalArgumentException.class, () -> {
            screen.set_id(invalidId);
        });
    }
}
