// Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-3.5-turbo

package com.team.backend;

import org.bson.types.ObjectId;
import org.springframework.data.annotation.Id;
import java.util.ArrayList;

import org.junit.Before;
import org.junit.Test;
import static org.assertj.core.api.Assertions.assertThat;
import static org.awaitility.Awaitility.await;

public class Screen_getScreenName_f8aab6599c_Test {

    private Screen screen;

    @Before
    public void setup() {
        screen = new Screen();
        screen.setScreenName("Test Screen");
    }

    @Test
    public void testGetScreenName() {
        String expectedScreenName = "Test Screen";
        
        await().untilAsserted(() -> {
            assertThat(screen.getScreenName()).isEqualTo(expectedScreenName);
        });
    }

    @Test
    public void testGetScreenName_Null() {
        screen.setScreenName(null);
        
        await().untilAsserted(() -> {
            assertThat(screen.getScreenName()).isNull();
        });
    }

    private class Screen {
        @Id
        private ObjectId id;
        private String screenName;

        public String getScreenName() {
            return screenName;
        }

        public void setScreenName(String screenName) {
            this.screenName = screenName;
        }
    }
}
