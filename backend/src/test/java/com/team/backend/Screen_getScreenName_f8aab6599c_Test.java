// Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-3.5-turbo

package com.team.backend;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;
import static org.awaitility.Awaitility.await;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

public class Screen_getScreenName_f8aab6599c_Test {

    @Test
    public void testGetScreenName_Success() {
        Screen screen = mock(Screen.class);
        String expectedScreenName = "Test Screen";
        when(screen.getScreenName()).thenReturn(expectedScreenName);

        await().untilAsserted(() -> {
            String actualScreenName = screen.getScreenName();
            assertThat(actualScreenName).isEqualTo(expectedScreenName);
        });
    }

    @Test
    public void testGetScreenName_NullScreenName() {
        Screen screen = mock(Screen.class);
        String expectedScreenName = null;
        when(screen.getScreenName()).thenReturn(expectedScreenName);

        await().untilAsserted(() -> {
            String actualScreenName = screen.getScreenName();
            assertThat(actualScreenName).isNull();
        });
    }
}
