// Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

package com.team.backend;

import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.beans.factory.annotation.Autowired;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Assertions;
import org.mockito.Mockito;
import java.util.Arrays;
import java.util.List;

@SpringBootTest
public class Controller_getAllScreens_b85a4c05e8_Test {

    @Autowired
    private ScreenService service;

    @MockBean
    private ScreenRepository repository;

    @Test
    public void testGetAllScreens_success() {
        Screen screen1 = new Screen();
        Screen screen2 = new Screen();
        List<Screen> screens = Arrays.asList(screen1, screen2);

        Mockito.when(repository.findAll()).thenReturn(screens);

        List<Screen> result = service.getAllScreens();
        
        Assertions.assertNotNull(result, "Result should not be null");
        Assertions.assertEquals(screens.size(), result.size(), "Sizes of lists should match");
        Assertions.assertIterableEquals(screens, result, "List contents should appear in the same order");
    }

    @Test
    public void testGetAllScreens_empty() {
        List<Screen> screens = Arrays.asList();

        Mockito.when(repository.findAll()).thenReturn(screens);

        List<Screen> result = service.getAllScreens();
        
        Assertions.assertNotNull(result, "Result should not be null");
        Assertions.assertEquals(screens.size(), result.size(), "Sizes of lists should match");
    }

}
