// Test generated by RoostGPT for test roost-test using AI Type Azure Open AI and AI Model roost-gpt4-32k

package com.team.backend;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;
import com.team.backend.controller.ScreenController;
import com.team.backend.model.Screen;
import com.team.backend.repository.ScreenRepository;
import static org.mockito.Mockito.when;
import static org.junit.jupiter.api.Assertions.assertEquals;

import java.util.Arrays;
import java.util.List;

public class Controller_getAllScreens_b85a4c05e8_Test {

    @InjectMocks
    ScreenController screenController;

    @Mock
    ScreenRepository screenRepository;

    @BeforeEach
    public void init() {
        MockitoAnnotations.initMocks(this);
    }

    @Test
    public void testGetAllScreens() {
        Screen screen1 = new Screen();
        Screen screen2 = new Screen();
        when(screenRepository.findAll()).thenReturn(Arrays.asList(screen1, screen2));
        List<Screen> screens = screenController.getAllScreens();
        assertEquals(2, screens.size());
    }

    @Test
    public void testGetAllScreensEmpty() {
        List<Screen> emptyList = Arrays.asList();
        when(screenRepository.findAll()).thenReturn(emptyList);
        List<Screen> screens = screenController.getAllScreens();
        assertEquals(0, screens.size());
    }

}
