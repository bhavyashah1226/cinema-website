# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=a21296b4f6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_put_success.csv"
    fields:
      - id
      - category_id
      - category_name
      - name
      - photoUrls
      - tags_id
      - tags_name
      - status
    skipHeader: true

scenarios:
  - name: "Update existing pet"
    flow:
      - post:
          url: "/pet"
          beforeRequest: "logPetDetails"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            category:
                id: "{{ category_id }}"
                name: "{{ category_name }}"
            name: "{{ name }}"
            photoUrls:
              - "{{ photoUrls }}"
            tags: 
              - id: "{{ tags_id }}"
                name: "{{ tags_name }}"
            status: "{{ status }}"
          capture:
            - json: "$"
              as: "responseBody"
          expect:
            - statusCode: 200
            - hasProperty: ["id", "category", "name", "photoUrls", "tags", "status"]
          afterRequest: "logResponseDetails"
      - think: 1
