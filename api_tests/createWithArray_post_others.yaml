# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=b50615cd0e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "createWithArray_post_others.csv"
    fields:
      - "id"
      - "username"
      - "firstName"
      - "lastName"
      - "email"
      - "password"
      - "phone"
      - "userStatus"
    skipHeader: true
scenarios:
  - name: "Testing error cases for /user/createWithArray API"
    flow:
    - think: 5
    - log: "Test request with empty payload"
    - post:
        url: "/user/createWithArray"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        json: {}
        expect:
        - statusCode: 400
        - hasProperty: "message"
        - equals: 
            message: "Invalid input"
    - think: 5
    - log: "Test request with invalid url"
    - post:
        url: "/invalidURL"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        json: 
          id: "{{ id }}"
          username: "{{ username }}"
        expect:
        - statusCode: 404
    - think: 5
    - log: "Test request with unauthorized access"
    - post:
        url: "/user/createWithArray"
        headers:
          Authorization: "InvalidAuthKey"
        json: 
          id: "{{ id }}"
          username: "{{ username }}"
        expect:
        - statusCode: 401
        - hasProperty: "message"
        - equals: 
            message: "Unauthorized"
