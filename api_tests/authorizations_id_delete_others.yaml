# Test generated by RoostGPT for test credential-wallet-yamlTest using AI Type Open AI and AI Model gpt-4


# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=cb128291e8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Test non-successful API requests"
    flow:
      # Test case: DELETE request with incorrect URL
      - think: 1
      - delete:
          url: "/authorizations/{id}/invalid"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404

      # Test case: DELETE request with invalid authorization token
      - think: 1
      - delete:
          url: "/authorizations/{{ id }}"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 401

      # Test case: DELETE request with missing authorization token
      - think: 1
      - delete:
          url: "/authorizations/{{ id }}"
          expect:
            - statusCode: 401

      # Test case: DELETE request with invalid id
      - think: 1
      - delete:
          url: "/authorizations/invalid-id"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400

      # Test case: DELETE request with missing id
      - think: 1
      - delete:
          url: "/authorizations/"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
