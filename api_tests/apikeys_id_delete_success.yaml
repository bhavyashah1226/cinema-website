# Test generated by RoostGPT for test credential-wallet-yamlTest using AI Type Open AI and AI Model gpt-4


# Test generated for /apikeys/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=4affc26394


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "apikeys_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
  defaults:
    headers:
      Token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "API keys test scenario"
    flow:
      - get:
          url: "/apikeys/{{ id }}"
          # Description: Request to get the API key details
          # Expected outcome: The server should return a successful response with the API key details
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
          think: 1
      - post:
          url: "/apikeys"
          # Description: Request to create a new API key
          # Expected outcome: The server should return a successful response with the details of the new API key
          json:
            id: "{{ id }}"
          expect:
            - statusCode: 201
            - hasProperty: "id"
          think: 1
      - put:
          url: "/apikeys/{{ id }}"
          # Description: Request to update the details of an API key
          # Expected outcome: The server should return a successful response confirming the update
          json:
            id: "{{ id }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
          think: 1
      - delete:
          url: "/apikeys/{{ id }}"
          # Description: Request to delete an API key
          # Expected outcome: The server should return a successful response confirming the deletion
          expect:
            - statusCode: 200
          think: 1
