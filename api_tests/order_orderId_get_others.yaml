# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=74f49e1ef7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload: 
    path: "order_orderId_get_others.csv"
    fields:
      - "orderId"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Test invalid and edge case scenarios for getOrderById API"
    flow:
      - log: "Send a request with an invalid orderId"
        # This request should return a 400 response as the orderId supplied is invalid
        get:
          url: "/store/order/INVALID_ID"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        think: 2

      - log: "Send a request with orderId that does not exist"
        # This request should return a 404 as the order does not exist
        get:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 2

      - log: "Send request to invalid URL"
        # This should return a 404 as the URL does not exist
        get:
          url: "/store/non_existent/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 2
    