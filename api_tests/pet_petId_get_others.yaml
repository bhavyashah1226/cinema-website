# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=2c3a7f4837

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payloads:
  - name: "pet_petId_get_others.csv"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Invalid Payloads and authorization Failure tests"
    flow:
      - log: "Fetching a pet with invalid petId"
        get:
          url: "/pet/randomId"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals: 
                - "description"
                - "Invalid ID supplied"

      - think: 2

      - log: "Trying to fetch a pet without authorization token"
        get:
          url: "/pet/{{ $processEnvironment.PET_ID }}"
          headers:
            Authorization: "InvalidToken" # Invalid authorization token
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: 
                - "message"
                - "Unauthorized"
      
      - think: 2

      - log: "Searching for a pet, which does not exist"
        get:
          url: "/pet/999999" # Non-existent petId
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "description"
            - equals: 
                - "description"
                - "Pet not found"
