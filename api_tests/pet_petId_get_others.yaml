# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /pet/{petId}_get for http method type GET in artillery framework

# RoostTestHash=4dfd26d5a8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "pet_petId_get_others.csv"
    fields:
      - "petId"
    skipHeader: true
  processor: "./my-functions.js"

scenarios:
  - name: Non-success API requests scenario
    flow:
      # Test case with invalid petId
      - think: 3
      - get:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - hasProperty: "type"
            - equals: 
                - "type"
                - "error"
            - matchesRegexp:
                - "message"
                - "Pet not found"

      # Test case with missing authorization header
      - think: 3
      - get:
          url: "/v2/pet/{{ petId }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: 
                - "message"
                - "Missing Authorization Header"

      # Test case with incorrect endpoint URL
      - think: 3
      - get:
          url: "/v2/invalid/{{ petId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: 
                - "message"
                - "Endpoint not found"

