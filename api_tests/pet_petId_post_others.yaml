# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=99d9f02040

config:
  target: "{{ $processEnvironment.API_HOST }}"
  think: 1
  plugins:
    expect: {}
  payload:
    path: "pet_petId_post_others.csv"
    fields:
      - "name"
      - "status"
    skipHeader: true

scenarios:
  - name: 'test invalid conditions for updatePetWithForm API'
    flow:
      - think: 1
      - post:
          authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          url: "/pet/invalidId" #Mapping to path variable petId
          formData:
            name: "Tommy"
            status: "Unavailable" 
          log: "Test with invalid petId"
          expect:
            - statusCode: 405
            - hasProperty: "description"
      - think: 1
      - post:
          authorization: "Bearer invalid_token"
          url: "/pet/{{ petId }}" #Invalid Authorization  
          formData:
            name: "Tommy"
            status: "Unavailable"
          log: "Test with invalid auth token"
          expect:
            - statusCode: 405
            - hasProperty: "description"
      - think: 1
      - post:
          authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          url: "/pet/{{ petId }}"
          formData:
            name: "{{ name}}" #Invalid form data, wrong field
            status: "Unavailable"
          log: "Test with invalid form data"
          expect:
            - statusCode: 405
            - hasProperty: "description"
