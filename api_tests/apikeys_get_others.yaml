# Test generated by RoostGPT for test credential-wallet-yamlTest using AI Type Open AI and AI Model gpt-4


# Test generated for /apikeys_get for http method type GET in artillery framework

# RoostTestHash=9e57a3b0dd


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "apikeys_get_others.csv"
    fields:
      - "ApiKey"
    skipHeader: true

scenarios:
  - name: "API failure scenarios"
    flow:
      # Incorrect URL
      - get:
          url: "/apike"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 1

      # Invalid token
      - get:
          url: "/apikeys"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 401
          think: 1

      # Valid request but expecting wrong status code
      - get:
          url: "/apikeys"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 500
          think: 1

      # Valid request but expecting a non-existent header
      - get:
          url: "/apikeys"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - hasHeader: "NonExistentHeader"
          think: 1

      # Valid request but expecting a non-existent body property
      - get:
          url: "/apikeys"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - hasProperty: "NonExistentProperty"
          think: 1
