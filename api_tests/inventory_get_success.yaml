# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=a66f7079af

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "inventory_get_success.csv"
    fields:
      - inventory
    skipHeader: true

scenarios:
  - name: "Test for /store/inventory endpoint"
    flow:
      - log: "Sending a GET request to /store/inventory"
        get:
          url: "/store/inventory"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          capture:
            json: "$.inventory"
            as: "responseInventory"
          expect:
            - statusCode: 200 
            - hasProperty: "inventory"
           
      - think: 1

      - log: "Asserting the response"
        think: 1
