# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=a66f7079af

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: Store Inventory Test
    flow:
      - get:
          url: "/store/inventory"
          headers:
            # Authorization token sourced from environment variable.
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          beforeRequest: 
            log: 'Test store/inventory API with valid token and Fetch Inventory'
          expect:
            - statusCode: 200  # Expected status code
            # Quick check to confirm headers exist in response
            - hasProperty: 'headers'
            - hasProperty: 'body'  # Confirm the body exists
