# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=474b7881ac

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    - think: 2
  processors: []

scenarios:
  - flow:
    - log: "Testing for Invalid Username"
    - post:
        url: "/user/login?username=invalid&password=correctPassword"
        headers: 
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect: 
          - statusCode: 400
          - hasProperty: 'X-Rate-Limit'
          - hasProperty: 'X-Expires-After'
    - think: 2
    - log: "Testing for Invalid Password"
    - post:
        url: "/user/login?username=validUsername&password=invalid"
        headers: 
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: 'X-Rate-Limit'
          - hasProperty: 'X-Expires-After'
    - think: 2
    - log: "Testing for Incorrect URL"
    - post:
        url: "/user/invalidURL?username=valid&password=correctPassword"
        headers: 
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
    - think: 2
    - log: "Testing for Unauthorized Access"
    - post:
        url: "/user/login?username=valid&password=correctPassword"
        headers: 
          Authorization: "InvalidToken"
        expect:
          - statusCode: 401
          - hasProperty: 'X-Rate-Limit'
          - hasProperty: 'X-Expires-After'
