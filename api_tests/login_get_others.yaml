# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /user/login_get for http method type GET in artillery framework

# RoostTestHash=474b7881ac


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "login_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Testing invalid username and password"
        get:
          url: "/v2/user/login?username={{ username }}&password={{ password }}"
          expect:
            - statusCode: 401
          think: 1

      - log: "Testing empty username and password"
        get:
          url: "/v2/user/login?username=&password="
          expect:
            - statusCode: 400
          think: 1

      - log: "Testing non-existent endpoint"
        get:
          url: "/v2/user/nonexistent"
          expect:
            - statusCode: 404
          think: 1

      - log: "Testing invalid HTTP method"
        post:
          url: "/v2/user/login?username={{ username }}&password={{ password }}"
          expect:
            - statusCode: 405
          think: 1

      - log: "Testing unauthorized access"
        get:
          url: "/v2/user/login?username={{ username }}&password={{ password }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 403
            - hasHeader: "WWW-Authenticate"
          think: 1
