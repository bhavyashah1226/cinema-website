# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=c43742dac4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./processor.js"
  plugins:
    expect: {}
  payload:
    path: "findByTags_get_success.csv"
    fields:
      - tags
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - name: Find Pets by Tags
    flow:
      - get:
          url: "/pet/findByTags"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            tags: "{{ tags }}"
          capture:
            - json: "$[0].id"
              as: "petId"
          expect:
            - hasStatus: 200 
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
          log: "Get request sent to /pet/findByTags to find pets by tags"
      - think: 1
