# Test generated by RoostGPT for test credential-wallet-yamlTest using AI Type Open AI and AI Model gpt-4


# Test generated for /apikeys_post for http method type POST in artillery framework

# RoostTestHash=28a54f1579


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "apikeys_post_others.csv"
    fields:
      - "ApiKeyAuth"
    skipHeader: true

scenarios:
  - name: "API test scenario for non-success responses"
    flow:
      # Testing POST request with invalid URL
      - post:
          url: "/invalidurl"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          think: 1

      # Testing POST request with invalid payload
      - post:
          url: "/apikeys"
          json:
            invalidKey: "invalidValue"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
          think: 1

      # Testing POST request without authentication token
      - post:
          url: "/apikeys"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 401
          think: 1

      # Testing POST request with invalid authentication token
      - post:
          url: "/apikeys"
          headers:
            Content-Type: "application/json"
            Token: "invalidToken"
          expect:
            - statusCode: 403
          think: 1

      # Testing POST request with valid payload but without required fields
      - post:
          url: "/apikeys"
          json:
            key: "{{ key }}"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
          think: 1
