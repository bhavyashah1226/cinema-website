# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=354b987cbe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 300
      arrivalRate: 50
  plugins:
    expect: {}
  payload:
    path: "uploadImage_post_others.csv"
    fields:
      - "petId"
      - "additionalMetadata"
      - "file"
    skipHeader: true

scenarios:
  - name: "Pet Image Upload - Failure Tests"
    flow:
      - log: "Upload Pet Image Test - Start"

      - think: 1

      - post:
          url: "/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "multipart/form-data"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          capture:
            - json: "$"
          expect:
            - statusCode: 401 # Unauthorized
            - hasProperty: "message"

      - think: 1

      - post:
          url: "/pet/InvalidId/uploadImage"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "multipart/form-data"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          capture:
            - json: "$"
          expect:
            - statusCode: 404 # Not Found
            - hasProperty: "message"

      - think: 1

      - post:
          url: "/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "multipart/form-data"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: ""
          capture:
            - json: "$"
          expect:
            - statusCode: 400 # Bad Request
            - hasProperty: "message"

      - log: "Upload Pet Image Test - End"
