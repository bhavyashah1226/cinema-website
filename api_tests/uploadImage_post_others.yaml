# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /pet/{petId}/uploadImage_post for http method type POST in artillery framework

# RoostTestHash=354b987cbe


config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./processor.js"
  plugins:
    expect: {}
  payload:
    path: "uploadImage_post_others.csv"
    fields:
      - "petId"
      - "additionalMetadata"
      - "file"
    skipHeader: true
  environments:
    dev:
      variables:
        auth_token: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      # Test with an incorrect URL
      - post:
          url: "/v2/invalid/url"
          headers:
            Authorization: "Bearer {{ auth_token }}"
          expect:
            - statusCode: 404
          think: 1

      # Test with missing required path parameter
      - post:
          url: "/v2/pet/"
          headers:
            Authorization: "Bearer {{ auth_token }}"
          expect:
            - statusCode: 400
          think: 1

      # Test with invalid authorization token
      - post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
          think: 1

      # Test with missing required form parameter (file)
      - post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "Bearer {{ auth_token }}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
          expect:
            - statusCode: 400
          think: 1

      # Test with invalid form parameter value (non-existing file)
      - post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "Bearer {{ auth_token }}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "non_existing_file.jpg"
          expect:
            - statusCode: 400
          think: 1
