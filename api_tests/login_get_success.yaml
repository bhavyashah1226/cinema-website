# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=474b7881ac

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "login_get_success.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "User Login Scenario"
    flow:
      - log: "Sending a GET request with username and password to login user"
        get:
          url: "/user/login"
          headers: 
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs: 
            username: "{{ username }}"
            password: "{{ password }}"
          expect:
            - statusCode: 200
            # The response body is expected to be a string as per the API spec
            - bodySchema: { type: "string" }
            # Checking if the response headers have 'X-Rate-Limit' and 'X-Expires-After'
            - hasProperty: "headers.X-Rate-Limit"
            - hasProperty: "headers.X-Expires-After"
        think: 1
