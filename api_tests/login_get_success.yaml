# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=474b7881ac

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20 
  processor: "./myProcessor.js"
  payload:
    path: "login_get_success.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "User Login Test"
    flow:
      - log: "Starting User Login Test"
      - post: 
          url: "/user/login"
          json:
            username: "{{ username }}"
            password: "{{ password }}"
          headers: 
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: 'slug'
          beforeRequest: "myProcessor.loginTracker"
          capture: 
            - json: "$.slug"
              as: "slug"
        think: 5
      - log: "Ending User Login Test"
