# Test generated by RoostGPT for test credential-wallet-yamlTest using AI Type Open AI and AI Model gpt-4


# Test generated for /authorizations_post for http method type POST in artillery framework

# RoostTestHash=ed8c1b7bff


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "authorizations_post_others.csv"
    fields:
      - "email"
  plugins:
    expect: {}

scenarios:
  - name: "Testing non-successful API responses"
    flow:
      # Test with missing authorization header
      - post:
          url: "/authorizations"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 401
          think: 1

      # Test with invalid authorization token
      - post:
          url: "/authorizations"
          json:
            email: "{{ email }}"
          headers:
            Token: "invalid_token"
          expect:
            - statusCode: 401
          think: 1

      # Test with missing email in payload
      - post:
          url: "/authorizations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json: {}
          expect:
            - statusCode: 400
          think: 1

      # Test with invalid email format in payload
      - post:
          url: "/authorizations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "invalid_email"
          expect:
            - statusCode: 400
          think: 1

      # Test with incorrect URL
      - post:
          url: "/incorrect_url"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
          think: 1
