# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=6e4c4bbafe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 1
      name: "Test scenario for delete order API"
  payload:
    path: "./order_orderId_delete_success.csv"
    fields:
      - "orderId"
    skipHeader: true
  plugins:
    expect: {}
  
scenarios:
  - name: "Delete purchase order by ID"
    flow:
      - log: "Delete an order with an valid orderId"
        post:
          url: "/store/order/{{ orderId }}"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "orderId"
          think: 1

      - log: "Delete an order with an invalid orderId"
        post:
          url: "/store/order/12345"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
          think: 1

      - log: "Delete an order that doesn't exist"
        post:
          url: "/store/order/9999"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
          think: 1
