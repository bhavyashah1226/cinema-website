# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /store/order/{orderId}_delete for http method type DELETE in artillery framework

# RoostTestHash=6e4c4bbafe


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "order_orderId_delete_others.csv"
    fields:
      - "orderId"
    skipHeader: true

scenarios:
  - name: "Non-Success API Responses"
    flow:
      # Invalid OrderId
      - post:
          url: "/v2/store/order/9999"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
          think: 1

      # OrderId as String
      - post:
          url: "/v2/store/order/test"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
          think: 1

      # Missing Authorization Token
      - post:
          url: "/v2/store/order/{{ orderId }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
          think: 1

      # Invalid URL
      - post:
          url: "/v2/store/invalid/{{ orderId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
          think: 1
