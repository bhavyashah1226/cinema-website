# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=a66f7079af

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./tests/processor.js"
  ensure: 
    p95: 200
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payloads:
    - path: "inventory_get_others.csv"
      fields: [ "inventory_status", "inventory_quantity" ]
      skipHeader: true

scenarios:
  - name: "Testing /store/inventory API endpoint"
    flow:
      - think: 10
      - log: "Send request with invalid URL"
      - get:
          url: "/store/invalidURL"
          headers:
            authoriztion: "INVALIDTOKEN"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      
      - think: 10
      - log: "Send request to get inventory without authorization"
      - get:
          url: "/store/inventory"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            
      - think: 10
      - log: "Send request to get inventory with invalid authorization token"
      - get:
          url: "/store/inventory"
          headers:
            authoriztion: "INVALIDTOKEN"
          expect:
            - statusCode: 403
            - hasProperty: "message"
            
      - think: 10
      - log: "Send request to get inventory with valid authorization but invalid inventory status"
      - get:
          url: "/store/inventory"
          headers:
            authoriztion: "{{ $processEnvironment.AUTH_TOKEN }}"
          beforeRequest: "setInvalidInventoryStatus"
          expect:
            - statusCode: 400
            - hasProperty: "message"
