# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=a66f7079af

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./my_processor.js"
  payload:
    path: "data/inventory_get_others.csv"
    fields:
      - "status"
    skipHeader: true 
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
scenarios:
  - name: "Get store inventory spec edge cases"
    flow: 
    - log: "Test case 1: Request with invalid URL"
    - get:
        url: "/invalid/inventory" 
        headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect: 
        - statusCode: 404
        - hasProperty: "message"
    - think: 5 

    - log: "Test case 2: Request without Authorization"
    - get:
        url: "/store/inventory"
        expect: 
        - statusCode: 401
        - hasProperty: "message"
    - think: 5 

    - log: "Test case 3: Request with wrong Authorization"
    - get:
        url: "/store/inventory"
        headers:
            Authorization: "invalid_authorization"
        expect: 
        - statusCode: 403
        - hasProperty: "message"
    - think: 5 
