# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /store/inventory_get for http method type GET in artillery framework

# RoostTestHash=a66f7079af


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "inventory_get_others.csv"
    fields:
      - "api_key"
    skipHeader: true

scenarios:
  - name: "Non-success API responses test"
    flow:
      # Request with invalid URL
      - get:
          url: "/v3/store/inventory"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
        think: 1

      # Request without required header
      - get:
          url: "/v2/store/inventory"
          expect:
            - statusCode: 401
        think: 1

      # Request with invalid header
      - get:
          url: "/v2/store/inventory"
          headers:
            api_key: "invalid_token"
          expect:
            - statusCode: 403
        think: 1

      # Request with valid header and check for invalid response
      - get:
          url: "/v2/store/inventory"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "invalid_property"
        think: 1
