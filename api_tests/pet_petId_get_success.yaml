# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=2c3a7f4837

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_get_success.csv"
    fields:
      - "petId"
    skipHeader: true

scenarios:
  - name: "Testing /pet/{petId} endpoint"
    flow:
      - log: "Starting petById request"
      - get:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/x-www-form-urlencoded"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasHeader: "content-type"
      - log: "Completed petById request, now waiting for 1 second"
      - think: 1
