# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /pet/{petId}_get for http method type GET in artillery framework

# RoostTestHash=4dfd26d5a8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_get_success.csv"
    fields:
      - "petId"
    skipHeader: true
  variables:
    auth_token: "{{ $processEnvironment.AUTH_TOKEN }}"
      
scenarios:
  - name: "Pet API Tests"
    flow:
      - # Request to get pet by ID
        get:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "{{ auth_token }}"
            petstore_auth: "{{ auth_token }}"
        expect:
          - hasProperty: "id"
          - hasProperty: "category"
          - hasProperty: "name"
          - hasProperty: "photoUrls"
          - hasProperty: "tags"
          - hasProperty: "status"
          - statusCode: 200
        think: 1
