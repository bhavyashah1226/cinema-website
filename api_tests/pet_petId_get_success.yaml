# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=2c3a7f4837

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "pet_petId_get_success.csv"
    fields:
      - "petId"
    skipHeader: true
scenarios:
  - name: "Find pet by ID"
    flow:
      - get:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/x-www-form-urlencoded"
          capture:
            - json: "$"
              as: "responseBody"
          expect:
            - statusCode: 200
            - hasProperty: "responseBody.name"
            - hasProperty: "responseBody.photoUrls"
          log: "Request to get pet details by petId"
        think: 1
