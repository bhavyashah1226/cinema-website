# Test generated by RoostGPT for test credential-wallet-yamlTest using AI Type Open AI and AI Model gpt-4


# Test generated for /credentials/{id}_get for http method type GET in artillery framework

# RoostTestHash=c02d3561ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "credentials_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Testing Non-Success API Responses"
    flow:
      # Testing GET request with incorrect URL
      - get:
          url: "/wrong/url/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404

      - think: 1

      # Testing GET request with valid URL but without auth token
      - get:
          url: "/credentials/{{ id }}"
          expect:
            - statusCode: 401

      - think: 1

      # Testing GET request with invalid auth token
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Token: "invalid_token"
          expect:
            - statusCode: 403

      - think: 1

      # Testing GET request with valid URL and auth token but invalid path parameter
      - get:
          url: "/credentials/invalid_id"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400

      - think: 1

      # Testing GET request with valid URL, auth token and path parameter
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "created_at"
            - hasProperty: "updated_at"

      - think: 1
