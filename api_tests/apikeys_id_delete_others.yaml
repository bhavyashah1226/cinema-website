# Test generated by RoostGPT for test credential-wallet-yamlTest using AI Type Open AI and AI Model gpt-4


# Test generated for /apikeys/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=4affc26394


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "apikeys_id_delete_others.csv"
    fields: ["id"]
    skipHeader: true

scenarios:
  - name: "Test various non-successful API requests"
    flow:
      # Testing with incorrect URL
      - get:
          url: "/incorrect/url"
          expect:
            - statusCode: 404
          think: 1

      # Testing with missing authorization token
      - post:
          url: "/apikeys/{{ id }}"
          headers:
            Token: ""
          expect:
            - statusCode: 401
          think: 1

      # Testing with invalid payload
      - post:
          url: "/apikeys/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalidKey: "invalidValue"
          expect:
            - statusCode: 400
          think: 1

      # Testing with missing required field in payload
      - post:
          url: "/apikeys/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: ""
          expect:
            - statusCode: 400
          think: 1

      # Testing with invalid authorization token
      - post:
          url: "/apikeys/{{ id }}"
          headers:
            Token: "invalidToken"
          json:
            name: "validName"
          expect:
            - statusCode: 403
          think: 1

      # Testing with valid payload and authorization but non-existing resource
      - post:
          url: "/apikeys/nonExistingId"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "validName"
          expect:
            - statusCode: 404
          think: 1
