# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=8a6351bbc5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "findByStatus_get_success.csv"
    skipHeader: true
    fields: 
      - "status"
      
scenarios:
  - name: "Find pets by status scenario"
    flow:
      - get:
          url: "/pet/findByStatus"
          headers: 
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          qs:
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasProperty: 'name'
            - hasProperty: 'photoUrls'
            - hasHeader: 'Content-Type'
          log: "Sent a GET request to /pet/findByStatus with status: {{ status }}"
          think: 1
