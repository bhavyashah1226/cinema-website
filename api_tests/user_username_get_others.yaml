# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=da281fe471

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "./user_username_get_others.csv"
    fields:
      - "username"
    skipHeader: true

scenarios:
  - name: "Test Failure Edge and Error cases for getUser API"
    flow:
      - think: 3
      - get:
          url: "/user/invalidUsername"
          headers:
            Authorization: "Bearer invalidToken"
          beforeRequest: logThis
          capture:
            json: "$"
          expect:
            - statusCode: 400
            - hasProperty: error
            - equals:
                message: "Invalid username supplied"
      - think: 3
      - get:
          url: "/user/notExistUsername"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          beforeRequest: logThis
          capture:
            json: "$"
          expect:
            - statusCode: 404
            - hasProperty: error
            - equals:
                message: "User not found"

js:
  function logThis(requestParams, context, ee, next) {
    console.log("Sending a request to ", requestParams.url);
    return next(); 
  }
