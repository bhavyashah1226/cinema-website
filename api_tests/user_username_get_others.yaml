# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=da281fe471

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "user_username_get_others.csv"
    fields:
      - "username"
    skipHeader: true

scenarios:
  - name: "Get User by Name failure scenarios"
    flow:
      - think: 1

      - log: "Failure scenario: Sending request with not found username"
        post: "/user/{{ username }}"
        headers:
          Content-Type: "application/json"
        expect:
          - statusCode: 404
          - hasProperty: "message"
          - equals: 
              message: "User not found"
        # Invalid username is supplied from CSV file hence user not found error is expected

      - think: 1

      - log: "Failure scenario: Sending request with invalid URL"
        post: "/invalidURL/{{ username }}"
        headers:
          Content-Type: "application/json"
        expect:
          - statusCode: 400
          - hasProperty: "message"
          - equals: 
              message: "Invalid URL"
        # Invalid URL is passed hence 400 Bad Request is expected

      - think: 1

      - log: "Failure scenario: Sending request without Content-Type header"
        post: "/user/{{ username }}"
        headers:
          Accept: "application/json"
        expect:
          - statusCode: 415
          - hasProperty: "message"
          - equals: 
              message: "Unsupported Media Type"
        # Sending request without Content-Type header, hence Unsupported Media Type is expected

      - think: 1

      - log: "Failure scenario: Sending request with invalid username and authorization token"
        post: "/user/{{ username }}"
        headers:
          Content-Type: "application/json"
          Authorization: "Bearer invalid_token"
        expect:
          - statusCode: 403
          - hasProperty: "message"
          - equals: 
              message: "Forbidden"
        # Invalid Authorization token is passed hence 403 Forbidden is expected
