# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /pet/findByStatus_get for http method type GET in artillery framework

# RoostTestHash=9575439991


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "findByStatus_get_others.csv"
    fields:
      - "status"
    skipHeader: true

scenarios:
  - name: "Non-success API responses test"
    flow:
      # Testing GET request with invalid status value
      - get:
          url: "/v2/pet/findByStatus?status={{ status }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
          think: 1

      # Testing GET request with no status value
      - get:
          url: "/v2/pet/findByStatus"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
          think: 1

      # Testing GET request with unauthorized token
      - get:
          url: "/v2/pet/findByStatus?status={{ status }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "message"
          think: 1

      # Testing GET request with non-existing URL
      - get:
          url: "/v2/pet/non-existing-url?status={{ status }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
          think: 1
