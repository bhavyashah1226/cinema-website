# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=8a6351bbc5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "./findByStatus_get_others.csv"
    skipHeader: true
    fields: 
      - "name"
      - "status"
  defaults:
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Testing failure cases, edge cases, error handling for findPetsByStatus API"
    flow: 
      - log: "Execution of incorrect HTTP method - POST instead of GET"
        post:
          url: "/pet/findByStatus?status=sold"
          beforeRequest:
          expect:
            - statusCode: 405
            - hasProperty: "name"
        think: 1

      - log: "Request with incorrect query parameters - unknown status"
        get:
          url: "/pet/findByStatus?status=unknown"
          expect:
            - statusCode: 400
            - hasProperty: "name"
        think: 1
      
      - log: "Request with invalid headers - missing 'Authorization'"
        get:
          url: "/pet/findByStatus?status=pending"
          headers:
            Authorization: "INVALID_TOKEN"
          expect:
            - statusCode: 401
            - hasProperty: "message"
        think: 1
