# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=8a6351bbc5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  payload:
    path: "findByStatus_get_others.csv"
    fields:
      - "status"
    skipHeader: true
  plugins:
    expect: {}
      
scenarios:
  - name: "Find pets by status with non success test cases"
    flow:

      # Request with missing authorization header
      - log: "Checking behavior with missing authorization"
        get:
          url: "/pet/findByStatus?status={{ status }}"
        expect:
          - statusCode: 403
          - hasProperty: "message"
        think: 2

      # Request with invalid authorization value
      - log: "Checking behavior with invalid authorization value"
        get:
          url: "/pet/findByStatus?status={{ status }}"
          headers:
            Authorization: "Bearer invalid_token"
        expect:
          - statusCode: 403
          - hasProperty: "message"
        think: 2

      # Request with valid authorization but non-existing pet status
      - log: "Checking behavior with non-existing pet status"
        get:
          url: "/pet/findByStatus?status=non_existing_status"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: "message"
        think: 2

      # Request with valid authorization but invalid request url
      - log: "Checking behavior with invalid request url"
        get:
          url: "/invalid_url?status=available"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        think: 2
