# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=16cf30f894

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "pet_post_success.csv"
    fields:
      - "id"
      - "category_id"
      - "category_name"
      - "name"
      - "photoUrls"
      - "tag_id"
      - "tag_name"
      - "status"
    skipHeader: true

scenarios:
  - name: "Test add a new pet to the store API"
    flow:
      - post:
          url: "/pet"
          headers: 
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
             id: "{{ id }}"
             category:
               id: "{{ category_id }}"
               name: "{{ category_name }}"
             name: "{{ name }}"
             photoUrls: ["{{ photoUrls }}"]
             tags:
               - id: "{{ tag_id }}"
                 name: "{{ tag_name }}"
             status: "{{ status }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "id"
            - hasProperty: "category"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "tags"
            - hasProperty: "status"
          capture:
            - json: "$"
              as: "responseBody"
          log: "Add a new pet to the store"
      - think: 1
