# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=16cf30f894

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    - expect: {}
  processor: ""
  payload:
    path: "pet_post_others.csv"
    fields: ["id", "name", "category", "tags", "status"]
    skipHeader: true

scenarios:
  - name: Pet Store API fail cases, edge cases, and error handling.
    flow:

      # Creating a request without a necessary payload
      - post:
          url: "/pet"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            photoUrls: ["samplephoto.com"]
          expect:
            - statusCode: 405
            - hasProperty: "error"
          log: "Request failed as required fields 'name', 'photoUrls' is missing in payload"


      # Invalid URL test case
      - think: 1
      - post:
          url: "/invalid_pet"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            category: "{{ category }}"
            name: "{{ name }}"
            photoUrls: ["samplephoto.com"]
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
            
          log: "Invalid URL test case failed as expected"

      # Authorization failure test case
      - think: 1
      - post:
          url: "/pet"
          headers:
            Authorization: "Invalid_Auth_Token"
          json:
             id: "{{ id }}"
             category: "{{ category }}"
             name: "{{ name }}"
             photoUrls: ["samplephoto.com"]
             tags: "{{ tags }}"
             status: "{{ status }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
          log: "Request failed due to Invalid Auth Token provided"

