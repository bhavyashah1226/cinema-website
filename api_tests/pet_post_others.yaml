# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=16cf30f894

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  environments:
    dev:
      target: "http://dev.target.domain"
      phases:
        - duration: 60
          arrivalRate: 20
  payload:
    path: "./pet_post_others.csv"
    skipHeader: true
    fields:
      - "id"
      - "name"
      - "status"

scenarios:
  - name: "Testing /pet API endpoint"
    flow:
      - log: "Testing API with invalid URL"
        post:
          url: "/invalidURL"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_INVALID_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 405
            - hasProperty: "description"
      - think: 2
      - log: "Testing API without required fields in the payload"
        post:
          url: "/pet"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            status: "{{ status }}"
          expect:
            - statusCode: 405
            - hasProperty: "description"
      - think: 2
      - log: "Testing API with invalid token"
        post:
          url: "/pet"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_INVALID_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 405
            - hasProperty: "description"
