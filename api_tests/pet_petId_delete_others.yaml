# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=fbd3e66b3b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_petId_delete_others.csv"
    fields:
      - "petId"
    skipHeader: true
  processor: "./processor.js"
  plugins:
    expect: {}

scenarios:
  - name: "Delete pet endpoint failure and edge case testing"
    flow:
      - log: "Testing with invalid petId"
        delete:
          url: "/pet/xyz"  # Invalid petId
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400 
            - hasProperty: "error"
            - equals: 
                - "error": "Invalid pet value"

      - think: 5  

      - log: "Testing with unauthorized access on deletePet"
        delete:
          url: "/pet/{{ petId }}"  # Using petId from payload csv
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer random_invalid_token"
          expect:
            - statusCode: 403
            - hasProperty: "error"
            - equals: 
                - "error": "Forbidden"

      - think: 5

      - log: "Testing with missing Authorization header"
        delete:
          url: "/pet/{{ petId }}"  # Using petId from payload csv
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals: 
                - "error": "Unauthorized"
