# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /pet/{petId}_delete for http method type DELETE in artillery framework

# RoostTestHash=fbd3e66b3b


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_petId_delete_others.csv"
    fields:
      - "petId"
    skipHeader: true
  environments:
    prod:
      target: "https://api.example.com"
scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Delete request with invalid petId"
        delete:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
        think: 1
      - log: "Delete request with missing auth token"
        delete:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: ""
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
        think: 1
      - log: "Delete request with invalid URL"
        delete:
          url: "/v2/invalid/{{ petId }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
        think: 1
