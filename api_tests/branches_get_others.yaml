# Test generated by RoostGPT for test credential-wallet-yamlTest using AI Type Open AI and AI Model gpt-4


# Test generated for /recipients/branches_get for http method type GET in artillery framework

# RoostTestHash=74a40da00e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
      name: "Simulate 2 users arriving every 1 second for 1 min"
  payload:
    path: "branches_get_others.csv"
    fields:
      - "coc-number"
      - "coc-branch-number"
      - "trade-name"
      - "size"
      - "offset"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Test non-successful API responses"
    flow:
      # First request tests an invalid URL
      - get:
          url: "/invalidURL"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      # Second request tests a GET request with invalid query parameters
      - get:
          url: "/recipients/branches?coc-number={{ coc-number }}&coc-branch-number={{ coc-branch-number }}&trade-name={{ trade-name }}&size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1

      # Third request tests a GET request without authorization
      - get:
          url: "/recipients/branches?coc-number={{ coc-number }}&coc-branch-number={{ coc-branch-number }}&trade-name={{ trade-name }}&size={{ size }}&offset={{ offset }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
        think: 1

      # Fourth request tests a GET request with expired authorization
      - get:
          url: "/recipients/branches?coc-number={{ coc-number }}&coc-branch-number={{ coc-branch-number }}&trade-name={{ trade-name }}&size={{ size }}&offset={{ offset }}"
          headers:
            Token: "expired_token"
          expect:
            - statusCode: 401
            - hasProperty: "message"
        think: 1
