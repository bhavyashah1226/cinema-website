# Test generated by RoostGPT for test credential-wallet-yamlTest using AI Type Open AI and AI Model gpt-4


# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=f1fc1d06f7


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "credentials_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Testing non-success API responses"
    flow:
      # Testing with invalid URL
      - get:
          url: "/invalid/url"
          expect:
            - statusCode: 404

      # Testing with missing authorization token
      - post:
          url: "/credentials/{{ id }}"
          headers:
            Token: ""
          expect:
            - statusCode: 401

      # Testing with invalid authorization token
      - post:
          url: "/credentials/{{ id }}"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 403

      # Testing with missing id in the URL
      - delete:
          url: "/credentials/"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400

      # Testing with invalid id in the URL
      - delete:
          url: "/credentials/invalidID"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404

      # Testing with unauthorized access to delete a credential
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 403

      # Think time after each request
      - think: 5
