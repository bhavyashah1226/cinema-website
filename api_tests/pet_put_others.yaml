# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=a21296b4f6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 2
  plugins:
    expect: {}
  payload:
    path: "pet_put_others.csv"
    fields:
      - "id"
      - "category"
      - "name"
      - "photoUrls"
      - "tags"
      - "status"
    skipHeader: true
scenario:
  name: "Test the /pet endpoint"
  flow:
    - log: "Testing the scenario with invalid id"
      put:
        url: "/badPet"
        json:
          id: "badId"
          category: "category"
          name: "name"
          photoUrls: "photoUrls"
          tags: "tags"
          status: "status"
        headers:
          Authorization: "{{ $processEnvironment.INVALID_AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: "description"
    - think: 1
    - log: "Testing with missing name and photoUrls (required properties)"
      put:
        url: "/pet"
        json:
          id: "id"
          category: "category"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 405
          - hasProperty: "description"
    - think: 1
    - log: "Testing with an invalid status"
      put:
        url: "/pet"
        json:
          id: "id"
          category: "category"
          name: "name"
          photoUrls: "photoUrls"
          tags: "tags"
          status: "badStatus"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 405
          - hasProperty: "description"
    - think: 1
    - log: "Testing with missing authorization token"
      put:
        url: "/pet"
        json:
          id: "id"
          category: "category"
          name: "name"
          photoUrls: "photoUrls"
          tags: "tags"
          status: "status"
        expect:
          - statusCode: 401
    - think: 1
    - log: "Testing with invalid token"
      put:
        url: "/pet"
        json:
          id: "id"
          category: "category"
          name: "name"
          photoUrls: "photoUrls"
          tags: "tags"
          status: "status"
        headers:
          Authorization: "{{ $processEnvironment.INVALID_AUTH_TOKEN }}"
        expect:
          - statusCode: 403
          - hasProperty: "description"
