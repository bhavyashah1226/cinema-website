# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=f6e646c39e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "./user_username_put_success.csv"
    fields:
      - id
      - username
      - firstName
      - lastName
      - email
      - password
      - phone
      - userStatus
    skipHeader: true

scenarios:
  - name: "Update User Test Scenario"
    flow:
      - log: "PUT /user/{username} Request Starts"
      - put:
          url: "/user/{{ username }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 200
            - headers:
                content-type: "application/json"
      - log: "PUT /user/{username} Request Ends"
      - think: 1
