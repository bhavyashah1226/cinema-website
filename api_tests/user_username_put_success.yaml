# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=f6e646c39e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./processors.js"
  variables:
    testApiKey: "{{ $processEnvironment.TEST_API_KEY }}"
    authToken: "{{ $processEnvironment.AUTH_TOKEN }}"
  payload:
    path: "./payloads/user_username_put_success.csv"
    fields: ["id", "username", "firstName", "lastName", "email", "password", "phone", "userStatus"]
    skipHeader: true
  ensure:
    - think: 1
  plugins:
    expect: {}
scenarios:
  - name: Update user scenario
    flow:
      - log: "Testing PUT /user/{username} with valid payload"
      - put:
          url: "/user/{{ username }}"
          headers: 
            Authorization: "Bearer {{ authToken }}"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 200
            - hasProperty: 'username'
            - hasProperty: 'email'
      - think: 1
      - log: "Testing POST /user/{username} with same payload - expecting conflict(409)"
      - post:
          url: "/user/{{ username }}"
          headers:
            Authorization: "Bearer {{ authToken }}"
          expect:
            - statusCode: 409
      - think: 1
