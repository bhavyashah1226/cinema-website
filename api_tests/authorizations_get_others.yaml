# Test generated by RoostGPT for test credential-wallet-yamlTest using AI Type Open AI and AI Model gpt-4


# Test generated for /authorizations_get for http method type GET in artillery framework

# RoostTestHash=3b64f22044


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_get_others.csv"
    fields:
      - "Token"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5
  variables:
    auth: "{{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "Testing non-success API responses"
    flow:
      # Testing with invalid API key
      - get:
          url: "/authorizations"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
          think: 1
      # Testing with missing API key
      - get:
          url: "/authorizations"
          headers:
            Token: ""
          expect:
            - statusCode: 401
            - hasProperty: "error"
          think: 1
      # Testing with valid API key but invalid endpoint
      - get:
          url: "/invalidEndpoint"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
          think: 1
      # Testing with valid API key and endpoint but invalid method
      - post:
          url: "/authorizations"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 405
            - hasProperty: "error"
          think: 1
