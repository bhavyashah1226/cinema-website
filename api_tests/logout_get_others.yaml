# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=a64e3feb39

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: logout_get_others.csv
    fields:
      - "invalidAuth"
      - "otherVariable"
    skipHeader: true

scenarios:
  - name: "Logging out User - Error Cases"
    flow:
      - log: "Attempt with Missing Authorization"
        get:
          url: "/user/logout"
          # Invalid Authorization Header
          headers:
            Authorization: "InvalidAuth {{ invalidAuth }}"
          expect:
            - statusCode: 401 # Unauthorized
            - hasProperty: "message"
        afterResponse: "think"

      - log: "Attempt with invalid URL - Extra Query Params"
        get:
          url: "/user/logout?random=123"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # URL not found
            - hasProperty: "error"
        afterResponse: "think"

      - log: "Attempt with invalid URL - Different End Point"
        get:
          url: "/users/logout"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # URL not found
            - hasProperty: "error"
        afterResponse: "think"

      - log: "Attempt with invalid Accept header"
        get:
          url: "/user/logout"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Accept: "application/pdf" # Invalid Accept Header
          expect:
            - statusCode: 415 # Unsupported Media Type
            - hasProperty: "error"
        afterResponse: "think"

      - log: "Attempt with invalid Content-Type header"
        get:
          url: "/user/logout"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/pdf" # Invalid Content Type
          expect:
            - statusCode: 415 # Unsupported Media Type
            - hasProperty: "error"
