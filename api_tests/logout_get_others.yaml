# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /user/logout_get for http method type GET in artillery framework

# RoostTestHash=a64e3feb39


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "logout_get_others.csv"
    fields:
      - "userId"
      - "password"
    skipHeader: true

scenarios:
  - name: "Non-successful API scenarios"
    flow:
      # Invalid token
      - post:
          url: "/v2/user/logout"
          headers:
            Authorization: "Bearer InvalidToken"
          expect:
            - statusCode: 401
          think: 2

      # Correct token but invalid URL
      - post:
          url: "/v2/user/logout_incorrect"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 2

      # Missing required fields in the payload
      - post:
          url: "/v2/user/logout"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            password: "{{ password }}"
          expect:
            - statusCode: 400
          think: 2

      # Invalid data format in the payload
      - post:
          url: "/v2/user/logout"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            userId: "{{ userId }}"
            password: 123456 # Invalid format
          expect:
            - statusCode: 400
          think: 2

      # Non-existing user
      - post:
          url: "/v2/user/logout"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            userId: "nonExistingUserId"
            password: "{{ password }}"
          expect:
            - statusCode: 404
          think: 2
