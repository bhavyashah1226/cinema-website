# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=a64e3feb39

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./my_processor.js"
  plugins:
    expect: {}
  payload:
    path: "logout_get_others.csv"
    fields:
      - "username"
      - "password"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Testing failure cases, edge cases and error handling for /user/logout API"
    flow:
      - log: "Sending AUTHORIZED request to /user/logout API"
        post:
          url: "/user/logout"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - headers:
                'content-type': "application/json; charset=utf-8"
            - hasProperty: "error"
            - equals:
                - "error.message": "Unauthorized"
        think: 10

      - log: "Sending UNAUTHORIZED request to /user/logout API"
        post:
          url: "/user/logout"
          headers:
            Authorization: "{{ unauthorizedToken }}"
          expect: 
            - statusCode: 403
            - headers:
                'content-type': "application/json; charset=utf-8"
            - hasProperty: "error"
            - equals: 
                - "error.message": "Forbidden"
        think: 10

      - log: "Sending request to INVALID /user/logout API"
        post:
          url: "/invalid/logout"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect: 
            - statusCode: 404
            - headers:
                'content-type': "application/json; charset=utf-8"
            - hasProperty: "error"
            - equals: 
                - "error.message": "Not Found"
        think: 10

      - log: "Sending request with Invalid HTTP Method to /user/logout API"
        get:
          url: "/user/logout"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect: 
            - statusCode: 405
            - headers:
                'content-type': "application/json; charset=utf-8"
            - hasProperty: "error"
            - equals: 
                - "error.message": "Method Not Allowed"
        think: 10
