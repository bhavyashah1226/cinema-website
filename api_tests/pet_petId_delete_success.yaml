# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=fbd3e66b3b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "./pet_petId_delete_success.csv"
    skipHeader: true

scenarios:
  - name: "Delete pet scenario testing"
    flow:
      - think: 5
      - post: 
          url: "/pet/{{ petId }}"
          headers:
            Content-Type: "application/x-www-form-urlencoded"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api_key: "{{ api_key }}"
          beforeRequest:
            log: "Starting to delete a pet with petId: {{ petId }}"
          expect:
            - statusCode: 200
            - hasProperty: 'message'
          afterResponse:
            log: "Pet with petId: {{ petId }} was deleted successfully"
          think: 5
