# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=da281fe471

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payloads:
    - path: "./user_username_get_success.csv"
      fields:
        - "username"
      skipHeader: true
scenarios:
  - name: "Get user by user name"
    flow:
      - log: "Fetching user details for '{{ username }}'"
      - get:
          url: "/user/{{ username }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "username"
            - hasProperty: "firstName"
            - hasProperty: "lastName"
            - hasProperty: "email"
            - hasProperty: "password"
            - hasProperty: "phone"
            - hasProperty: "userStatus"
          afterResponse: 'logResponse'
      - think: 1

# Expected to return the user's details in the response.
# The response contains a header.
# The status code is 200 from the server if the operation is successful.
# The response body contains id, username, firstName, lastName, email, password, phone, userStatus fields.
# Invalid username or User not found errors should return the respective error message and error code.
