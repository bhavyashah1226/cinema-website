# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=70af040573

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./myProcessor.js"
  payload:
    path: "createWithList_post_others.csv"
    fields:
      - "id"
      - "username"
      - "firstName"
      - "lastName"
      - "email"
      - "password"
      - "phone"
      - "userStatus"
    skipHeaders: true
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}

scenarios:
  - name: "Create user with list Error Handling."
    flow:
      - log: "Invalid URL Error"
        post:
          url: "/invalidurl"
          headers:
            Authorization: "Basic invalidtoken"
          expect:
            - statusCode: 404

      - think: 1

      - log: "Invalid Authorization Error"
        post:
          url: "/user/createWithList"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          headers:
            Authorization: "Basic invalidtoken"
          expect:
            - hasProperty: "message"
            - statusCode: 401

      - think: 1

      - log: "Missing required field error"
        post:
          url: "/user/createWithList"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasProperty: "message"
            - statusCode: 400

      - think: 1

      - log: "Invalid content type error"
        post:
          url: "/user/createWithList"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "text/plain"
          expect:
            - hasProperty: "message"
            - statusCode: 415

      - think: 1
