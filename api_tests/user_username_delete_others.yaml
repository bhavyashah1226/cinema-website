# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=d8125b8dc9

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_username_delete_others.csv"
    fields:
      - "username"
    skipHeader: true

scenarios:
  - name: "Error case scenario for DELETE user API"
    flow:
      - log: "Firstly, testing for invalid username"
        post:
          url: "/user/{{ username }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 400
            - headerContains: 'application/json'
      - log: "Now, a think time of 5s before next request"
        think: 5
      - log: "Testing with correct username but no user found scenario"
        post: 
          url: "/user/{{ username }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - headerContains: 'application/json'
