# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /user/{username}_delete for http method type DELETE in artillery framework

# RoostTestHash=d8125b8dc9


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "user_username_delete_others.csv"
    fields:
      - "username"
    skipHeader: true

scenarios:
  - name: "Invalid API usage scenario"
    flow:
      # Invalid token
      - log: "Invalid token scenario"
        delete:
          url: "/v2/user/{{ username }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
        think: 1

      # Non-existent user
      - log: "Non-existent user scenario"
        delete:
          url: "/v2/user/nonexistentuser"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      # Incorrect URL
      - log: "Incorrect URL scenario"
        delete:
          url: "/v2/user/incorrect/url/{{ username }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      # Missing username in URL
      - log: "Missing username in URL scenario"
        delete:
          url: "/v2/user/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 1
