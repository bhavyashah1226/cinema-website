# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=d8125b8dc9

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 1
      arrivalRate: 1
  ensure:
    p99: 2000
  plugins:
    expect: {}
  payload:
    path: "user_username_delete_others.csv"
    fields:
      - "username"
    skipHeader: true

scenarios:
  - name: "Delete user robustness tests"
    flow:
      - Think: 1
      - post:
          url: "/user/{{ username }}"
          headers: 
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: 'reason'
          log: "Checking invalid username case"
      - Think: 1
      - post:
          url: "/user/{{ username2 }}"
          headers: 
            authorization: "{{ $processEnvironment.AUTH_TOKEN2 }}"
          expect:
            - statusCode: 404
            - hasProperty: 'error'
          log: "Checking user not found case"
      - Think: 1
      - post:
          url: "/invalid/url"
          headers: 
            authorization: "{{ $processEnvironment.AUTH_TOKEN_WRONG }}"
          expect:
            - statusCode: 404
          log: "Checking invalid URL case"
      - Think: 1
      - post:
          url: "/user/{{ username }}"
          headers: 
            authorization: "invalid_auth_token"
          expect:
            - statusCode: 401
          log: "Checking invalid authorization case"
