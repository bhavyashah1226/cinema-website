# Test generated by RoostGPT for test credential-wallet-yamlTest using AI Type Open AI and AI Model gpt-4


# Test generated for /me_get for http method type GET in artillery framework

# RoostTestHash=ae6b8ce5ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "me_get_success.csv"
    fields:
      ["id", "did", "name", "image", "email", "settings", "createdAt", "updatedAt"]
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - name: Validate API behavior with valid input payloads
    flow:
      # First request to validate successful operation
      - get:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasHeader: "content-type"
            - headerEquals: {"content-type": "application/json"}
            - hasProperty: ["id", "did", "name", "image", "email", "settings", "createdAt", "updatedAt"]
            - equals: 
                - "id": "{{ id }}"
                - "did": "{{ did }}"
                - "name": "{{ name }}"
                - "image": "{{ image }}"
                - "email": "{{ email }}"
                - "settings": "{{ settings }}"
                - "createdAt": "{{ createdAt }}"
                - "updatedAt": "{{ updatedAt }}"
          think: 1
