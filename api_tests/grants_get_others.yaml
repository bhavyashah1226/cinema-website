# Test generated by RoostGPT for test credential-wallet-yamlTest using AI Type Open AI and AI Model gpt-4


# Test generated for /grants_get for http method type GET in artillery framework

# RoostTestHash=692c6b853f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "grants_get_others.csv"
    fields:
      - "Token"
    skipHeader: true

scenarios:
  - name: "API failure cases"
    flow:
      - post:
          url: "/invalidURL"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 2

      - get:
          url: "/grants"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 401
          think: 2

      - post:
          url: "/grants"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 401
          think: 2

      - put:
          url: "/grants"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            invalidField: "invalidValue"
          expect:
            - statusCode: 400
          think: 2

      - delete:
          url: "/grants"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 405
          think: 2
