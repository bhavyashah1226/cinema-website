# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=70af040573

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "createWithList_post_success.csv"
    fields:
      - "id"
      - "username"
      - "firstName"
      - "lastName"
      - "email"
      - "password"
      - "phone"
      - "userStatus"
    skipHeader: true
scenarios:
  - name: "POST /user/createWithList successful scenario"
    flow:
      - log: "Starting the POST /user/createWithList test"
      - post:
          url: "/user/createWithList"
          json:
            id            : "{{ id }}"
            username      : "{{ username }}"
            firstName     : "{{ firstName }}"
            lastName      : "{{ lastName }}"
            email         : "{{ email }}"
            password      : "{{ password }}"
            phone         : "{{ phone }}"
            userStatus    : "{{ userStatus }}"
          headers:
            Authorization : "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - header: 'Content-Type'
            - hasProperty: 'username'
            - hasProperty: 'firstName'
            - hasProperty: 'lastName'
            - hasProperty: 'email'
            - hasProperty: 'password'
            - hasProperty: 'phone'
            - hasProperty: 'userStatus'
          capture:
            - {json: "$", as: "body"}
      - think: 1
      - log: "POST /user/createWithList test finished"
