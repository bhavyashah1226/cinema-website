# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /user/logout_get for http method type GET in artillery framework

# RoostTestHash=a64e3feb39


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "logout_get_success.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true

scenarios:
  - name: "Logout user test scenario"
    flow:
      - post:
          url: "/v2/user/logout"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            username: "{{ username }}"
            password: "{{ password }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - equals: ["message", "User logged out successfully"]
      - think: 1
