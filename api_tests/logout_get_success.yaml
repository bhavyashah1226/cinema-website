# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=a64e3feb39

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Logout API test"
    flow:
      - log: "Sending a logout request"
        post:
          url: "/user/logout"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect: 
          - statusCode: 200
          - hasProperty: "message"
        think: 1
      - log: "Sending another logout request to verify sessions are not persistent"
        post:
          url: "/user/logout"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect: 
          - statusCode: 200
          - hasProperty: "message"
        think: 1
