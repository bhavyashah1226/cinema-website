# Test generated by RoostGPT for test credential-wallet-yamlTest using AI Type Open AI and AI Model gpt-4


# Test generated for /credentials_get for http method type GET in artillery framework

# RoostTestHash=c5011f3c64


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "credentials_get_success.csv"
    fields:
      - "id"
      - "format"
      - "credential"
      - "status"
      - "statusUpdatedAt"
      - "createdAt"
    skipHeader: true

scenarios:
  - name: "Test scenario for validating API behavior"
    flow:
      - get:
          url: "/credentials?size=20&offset=0"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
            - hasProperty: "total"
            - hasProperty: "credentials"
            - hasProperty: "credentials[0].id"
            - hasProperty: "credentials[0].format"
            - hasProperty: "credentials[0].credential"
            - hasProperty: "credentials[0].status"
            - hasProperty: "credentials[0].statusUpdatedAt"
            - hasProperty: "credentials[0].createdAt"
          capture:
            - json: "$.credentials[0].id"
              as: "id"
            - json: "$.credentials[0].format"
              as: "format"
            - json: "$.credentials[0].credential"
              as: "credential"
            - json: "$.credentials[0].status"
              as: "status"
            - json: "$.credentials[0].statusUpdatedAt"
              as: "statusUpdatedAt"
            - json: "$.credentials[0].createdAt"
              as: "createdAt"
      - think: 1
