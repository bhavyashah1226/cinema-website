# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=99d9f02040

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_post_success.csv"
    fields: ["petId", "name", "status"]
    skipHeader: true
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
      
scenarios:
  - name: "Scenario Name"
    flow:
      - post:
          url: "/pet/{{ petId }}"
          headers:
            Content-Type: "application/x-www-form-urlencoded"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - responseBody.hasProperty: "message"
            - responseBody.schema.hasProperties: ["id", "name", "status"]
            - header: "content-type"
          log: "Posting an update to pet with ID {{ petId }}"
          think: 5
