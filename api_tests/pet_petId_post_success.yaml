# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=99d9f02040

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_petId_post_success.csv"
    fields:
      - "petId"
      - "name"
      - "status"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
- name: "Update pet with form data"
  flow:
    - log: "Updates a pet in the store with form data"
      post:
        url: "/pet/{{ petId }}"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}" 
          Content-Type: "application/x-www-form-urlencoded"
        formData:
          name: "{{ name }}"
          status: "{{ status }}"
          # This request updates the pet details with its ID, Name and Status
        expect:
          - statusCode: 200
          - hasProperty: "pet"
          - hasProperty: "name"
          - hasProperty: "status"
        capture:
          - json: "$.petId"
            as: "petId"
          - json: "$.name"
            as: "name"
          - json: "$.status"
            as: "status"

    - think: 1
