# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=d8125b8dc9

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: user_username_delete_success.csv
    skipHeader: true
    fields:
      - username
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: User deletion Scenarios
    flow:
    - log: 'Initiating DELETE request for deleting user'
    - think: 5
    - delete:
        url: "/user/{{ username }}"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
    - log: 'Request Sent Successfully'
    - think: 1
    - expect:
        statusCode: 200
        headers:
          'Content-Type': 'application/json'
    - log: 'Expecting 200 status code and header containing application/json Content-Type'
    - think: 1
