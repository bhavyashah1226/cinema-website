# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=c43742dac4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 30
      arrivalRate: 1
  payload:
    path: "findByTags_get_others.csv"
    fields:
      - "tags"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "API failure scenarios"
    flow:
      - log: "Attempt to access API with invalid credential"
        get:
          url: "/pet/findByTags"
          headers:
            Authorization: "Bearer incorrect_token"
          qs:
            tags: "{{ tags }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - hasProperty: "code"
      - think: 1
      - log: "Attempt to access API with invalid url"
        get:
          url: "/pet/invalidUrl"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            tags: "{{ tags }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - hasProperty: "code"
      - think: 1
      - log: "Attempt to access API without required query parameters"
        get:
          url: "/pet/findByTags"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "code"
