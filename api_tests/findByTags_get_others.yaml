# Test generated by RoostGPT for test roostPostmanTest using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=c43742dac4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./customProcessor.js"
  payload:
    path: "findByTags_get_others.csv"
    fields: ["tags"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Failure Scenario"
    flow:
      - log: "Test - Invalid Endpoint"
        get:
          url: "/pet/invalid"
          headers:
            Authorization: "Basic {{ $processEnvironment.INVALID_AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "Error"
          beforeRequest: "add_comments"
        think: 5
      - log: "Test - Invalid Auth Token"
        get:
          url: "/pet/findByTags?tags={{ tags }}"
          headers:
            Authorization: "Basic {{ $processEnvironment.INVALID_AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "Unauthorized"
          beforeRequest: "add_comments"
        think: 5
      - log: "Test - Missing Parameter"
        get:
          url: "/pet/findByTags"
          headers:
            Authorization: "Basic {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "Invalid tag value"
          beforeRequest: "add_comments"
        think: 5
      - log: "Test - Invalid Method"
        post:
          url: "/pet/findByTags?tags={{ tags }}"
          headers:
            Authorization: "Basic {{ $processEnvironment.AUTH_TOKEN }}"
          expect: 
            - statusCode: 405
            - hasProperty: "Invalid method"
          beforeRequest: "add_comments"
        think: 5
