import csv
import uuid

header = ['id', 'statusCode']
data = []

for _ in range(10):
    id_ = uuid.uuid4()  # generates a random UUID
    status_code = 200  # an example status code, can be made dynamic
    data.append([str(id_), status_code])

with open('data.csv', 'w', newline='') as file:
    writer = csv.writer(file)
    writer.writerow(header)
    writer.writerows(data)
