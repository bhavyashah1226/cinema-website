import csv
import random

# Define a function to generate a random integer between a minimum and maximum value
def random_integer(minimum, maximum):
    return random.randint(minimum, maximum)

# Define headers
header = ["size", "offset", "statusCode"]

# Initialize data list with header
data = [header]

# Define number of rows
num_rows = 10

# Generate data rows
for i in range(num_rows):
    size = random_integer(1, 50)     # Size should be an integer between 1 and 50
    offset = random_integer(0, 100)  # Offset should be an integer greater than or equal to 0
    statusCode = random_integer(100, 600)  # Assuming statusCode to be HTTP status codes, ranging from 100 to 600.
    
    data.append([size, offset, statusCode])

# Write data to CSV file
with open("sample_data.csv", "w", newline="") as f:
    writer = csv.writer(f)
    writer.writerows(data)

# Print data
for row in data:
    print(", ".join([str(elem) for elem in row]))
