import csv
import uuid
from faker import Faker

fake = Faker()

def create_csv_file(file_name, field_names, num_rows):
    with open(file_name, mode='w') as csv_file:
        writer = csv.DictWriter(csv_file, fieldnames=field_names)

        writer.writeheader()
        for _ in range(num_rows):
            writer.writerow({
                'id': str(uuid.uuid4()),
                'statusCode': fake.pyint(min_value=100, max_value=599)
            })

create_csv_file('dummy.csv', ['id', 'statusCode'], 10)
