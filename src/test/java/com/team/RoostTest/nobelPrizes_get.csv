import csv
import random

# define constants
NUM_ROWS = 10
SORT_OPTIONS = ['asc', 'desc']
NOBEL_PRIZE_CATEGORIES = ['che', 'eco', 'lit', 'pea', 'phy', 'med']
FORMAT_OPTIONS = ['json', 'csv']
CSV_LANG_OPTIONS = ['en', 'se', 'no']

# generate random data for each variable
def generate_data():
    offset = random.randint(1, 100)
    limit = random.randint(1, 50)
    sort = random.choice(SORT_OPTIONS)
    nobelPrizeYear = random.randint(1901, 2021)
    yearTo = random.randint(nobelPrizeYear, 2021)
    nobelPrizeCategory = random.choice(NOBEL_PRIZE_CATEGORIES)
    format = random.choice(FORMAT_OPTIONS)
    csvLang = random.choice(CSV_LANG_OPTIONS)
    statusCode = random.randint(100, 500)
    
    return [offset, limit, sort, nobelPrizeYear, yearTo, nobelPrizeCategory, format, csvLang, statusCode]

# define column names
headers = ['offset', 'limit', 'sort', 'nobelPrizeYear', 'yearTo', 'nobelPrizeCategory', 'format', 'csvLang', 'statusCode']

# create and write to CSV file
with open('sample.csv', 'w', newline='') as f:
    writer = csv.writer(f)
    writer.writerow(headers)
    for _ in range(NUM_ROWS):
        data = generate_data()
        writer.writerow(data)
