# Importing necessary libraries
from faker import Faker
import pandas as pd

# Create a Faker instance
fake = Faker()

# Initialise a list to store all records 
data = []

# For loop to generate 10 random emails
for _ in range(10):
    data.append([fake.email(), fake.random_int(min=100, max=400)])

# Create DataFrame from the list
df = pd.DataFrame(data, columns=['email', 'statusCode'])

# Saving DataFrame as CSV file
df.to_csv('sample_data.csv', index=False, header=True)
