import csv
import uuid
import random


# CSV Headers
csv_headers = ['id', 'statusCode']

# id Schema Info
id_schema_info = {"schema": {"type": "string", "format": "uuid"}}

# statusCode is between 100 to 500
status_codes = list(range(100, 501))

# Total entries required
total_entries = 10

# CSV file name
csv_file_name = 'dummy_profile_data.csv'

# Data placeholder
data = []

# Generating dummy data
for _ in range(total_entries):

    # Generating uuid for field 'id'
    if id_schema_info['schema']['type'] == 'string' \
            and id_schema_info['schema']['format'] == 'uuid':
        _id = str(uuid.uuid4())
    else:
        _id = None

    # Randomly selecting statusCode from status_codes list
    statusCode = random.choice(status_codes)

    # Appending data to the list
    data.append([_id, statusCode])

# Writing data to CSV
with open(csv_file_name, 'w', newline='') as file:
    writer = csv.writer(file)
    # Writing headers
    writer.writerow(csv_headers)
    # Writing data
    writer.writerows(data)

print(str(total_entries) + " dummy profile data written successfully to " + csv_file_name)
